import { createTheme } from '@mui/material';

const Light = createTheme({
	palette: {
		mode: 'light',
		primary: {
			main: '#303034',
		},
		secondary: {
			main: '#424b97',
		},
	},
});

export default Light;
